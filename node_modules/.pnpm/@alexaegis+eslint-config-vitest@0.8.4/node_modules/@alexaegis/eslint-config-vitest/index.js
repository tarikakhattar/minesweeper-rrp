const index = {
  rules: {
    "no-restricted-imports": [
      "error",
      {
        paths: ["node:test", "test", "chai", "mocha", "jest"]
        // Prevent imports from other test frameworks
      }
    ]
  }
};
export {
  index as default
};
