"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("vitest");
const mockDefaultPrettifiedJsonOutput = "prettyJson";
const mockPrettier = (vi2) => {
  const mock = {
    format: vi2.fn(
      (_data) => new Promise((resolve) => {
        resolve(mockDefaultPrettifiedJsonOutput);
      })
    ),
    resolveConfig: vi2.fn()
  };
  return {
    prettier: mock,
    prettierMock: mock
  };
};
exports.mockDefaultPrettifiedJsonOutput = mockDefaultPrettifiedJsonOutput;
exports.mockPrettier = mockPrettier;
