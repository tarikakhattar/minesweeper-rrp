import { type Plugin } from 'unified';
/**
 * A unified plugin which reports inconsistencies between the input and what a pipeline would
 * output.
 */
declare const unifiedConsistency: Plugin;
export default unifiedConsistency;
