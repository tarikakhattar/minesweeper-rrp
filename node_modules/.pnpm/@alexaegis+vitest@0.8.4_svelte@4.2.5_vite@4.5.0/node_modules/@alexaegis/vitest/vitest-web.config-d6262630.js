import { svelte } from "@sveltejs/vite-plugin-svelte";
import { mergeConfig } from "vitest/config";
const vitestReporters = ["text", "json", "html", "lcov"];
const vitestCoverage = {
  provider: "v8",
  reporter: vitestReporters,
  reportsDirectory: "./coverage"
};
const vitestWebConfig = {
  plugins: [],
  test: {
    include: ["src/**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}"],
    globals: true,
    environment: "jsdom",
    coverage: vitestCoverage
  }
};
const vitestSvelteConfig = mergeConfig(vitestWebConfig, {
  plugins: [svelte({ hot: !process.env["VITEST"] })]
});
export {
  vitestSvelteConfig as a,
  vitestWebConfig as b,
  vitestCoverage as v
};
